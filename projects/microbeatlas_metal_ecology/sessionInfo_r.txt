R session information for NB04 (Pagel's λ) and NB05 (PGLS regression)
Captured: 2026-05-02

R version 4.5.2
Platform: x86_64-pc-linux-gnu

Packages used:
  ape          >= 5.7    # phylogenetic tree pruning (keep.tip), corPagel correlation structure
  phytools     >= 2.1    # phylogenetic signal (phylosig, Pagel's lambda)
  nlme         >= 3.1    # gls() generalised least squares (bundled with R base)

Key functions:
  ape::keep.tip()        — tree pruning to genus representatives
  phytools::phylosig()   — Pagel's lambda estimation (method='lambda')
  ape::gls()             — PGLS via corPagel correlation structure
  nlme::corPagel()       — Pagel's lambda correlation model

Installation (R console):
  install.packages(c("ape", "phytools"))
  # nlme is included with base R

Note: ape::corPagel behavior changed between ape 5.6 and 5.7 (fixed a sign error
in the lambda gradient). Results were generated with ape >= 5.7. Using earlier
versions may produce slightly different lambda estimates.
